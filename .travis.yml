os: linux
dist: xenial
sudo: true
compiler: gcc
language: cpp

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-8
      - g++-8
      - cmake

services:
  - docker

before_script:
  - echo $(pwd)
  - ls -la
  - docker build . --tag RPG_GAME_lin

jobs:
  include:
    - name: "tests"
      script:
        - docker run --rm -t -v $(pwd):/RPG_GAME_lin RPG_GAME_lin scripts/tests.sh
#        - git submodule update --init --recursive
#        - sudo apt-get install -y libfreetype6-dev libxrandr-dev libudev-dev libogg-dev libflac-dev libvorbis-dev libopenal-dev
#        - sudo ln -s /usr/bin/gcc-8 /usr/local/bin/gcc
#        - sudo ln -s /usr/bin/g++-8 /usr/local/bin/g++
#        - mkdir -p .build
#        - cd .build
#        - cmake .. -DBUILD_TESTS=On
#        - cmake --build .
    - name: "checks"
      script:
        - docker run -v `pwd`:`pwd` -w `pwd` --cap-add=SYS_PTRACE -t rusdevops/bootstrap-cpp scripts/checks.sh